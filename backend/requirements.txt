# --- Web framework -----------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20      # required by FastAPI for Form(...)/UploadFile (app/api/endpoints/documents.py)

# --- Database (async) ---------------------------------------------------------
sqlalchemy[asyncio]==2.0.36   # [asyncio] extra pulls in greenlet, required for async ORM
psycopg[binary]==3.2.3        # psycopg3 — supports both sync and async engines natively
pgvector==0.3.6                # pgvector type support for future SQLAlchemy models
alembic==1.14.0

# --- Configuration --------------------------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0        # required for pydantic's EmailStr (app/schemas/user.py)
python-dotenv==1.0.1

# --- Document parsing ---------------------------------------------------------
pypdf==5.1.0
python-docx==1.1.2

# --- Embeddings (Ollama HTTP API) ---------------------------------------------
httpx==0.28.1

# --- Testing ----------------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.25.0
