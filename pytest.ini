[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
testpaths = backend/tests
pythonpath = backend
norecursedirs = scratch backend/scratch .venv alembic
filterwarnings =
    ignore::RuntimeWarning
    ignore::DeprecationWarning:botocore
